# time_observable — candidate dynamic observables for the seconds/MCS calibration
# represented_by_model is judged against the FOUR terms actually in
# compute_delta_H (adhesion, volume, radiation, melanin). The CPM has
# motility and shape rearrangement but no nutrient-driven biomass
# creation, so growth-dominated observables are excluded regardless of
# how well they can be measured.
# semantically_compatible_with_parcel is the separate question the
# entity semantics force: a per-cell MSD is not a parcel MSD.
# An observable must satisfy ALL THREE to be selectable.
# UNIQUENESS KEY: observable_id
# Blank numeric cells mean NOT MEASURED. They are never read as zero.
#
# NONE is selected. dt_MCS = a^2 * S_sim / S_exp needs a pitch AND an
# observable, so seconds/MCS is blocked twice over.
observable_id,description,model_quantity,experimental_quantity,represented_by_model,measurable,semantically_compatible_with_parcel,selected,evidence_basis,source_id,status,notes
single_cell_msd,Mean-squared displacement of an individual tracked organism,Displacement of one cell_id centroid per MCS,Per-cell MSD from time-lapse tracking,true,true,false,false,declared,TIME_DECL_2026,blocked,"The conventional choice, and the one the earlier plan assumed. A cell_id is a computational biomass parcel, not an observable bacterium, so a per-cell MSD and a parcel MSD are not the same quantity. Usable only if a declared mapping from tracked cells to parcels is added."
biomass_autocorrelation_decay,Decay rate of the spatial biomass autocorrelation,Change in correlation length of the occupancy field per MCS,Correlation length of the segmented biomass field over time,true,true,true,false,declared,TIME_DECL_2026,provisional,"Strongest candidate: field-level on both sides, so it needs no cell-to-parcel mapping, and structure.correlation_length already computes it for the model side. Needs time-resolved 3D stacks, which is a harder acquisition than a single stack."
aggregate_centroid_displacement,Displacement of segmented aggregate centroids,Centroid motion of 26-connected components,Centroid motion of segmented aggregates between frames,true,true,true,false,declared,TIME_DECL_2026,provisional,"Aggregates are parcel-scale rather than cell-scale, so the semantics match. Requires aggregate identity to persist between frames, which segmentation alone does not guarantee."
interface_rearrangement_rate,Rate of change of the biomass-void interface,Change in specific interface area per MCS,Change in measured interface area over time,true,true,true,false,declared,TIME_DECL_2026,provisional,"Directly driven by the adhesion and volume terms, which is what the CPM's dynamics actually are. Sensitive to segmentation threshold, so it needs the occupancy mapping declared first."
radial_redistribution,Radial drift under a controlled gradient,Change in the radial biomass profile per MCS,Radial profile change under an imposed gradient,true,true,true,false,declared,TIME_DECL_2026,blocked,"Requires an experimental gradient that the CPM also represents. The nutrient field is written but never read by compute_delta_H, so today the model has no gradient response to match."
biofilm_thickening_rate,Rate of increase of biofilm thickness,none,Thickness increase over time,false,true,true,false,declared,TIME_DECL_2026,unsupported_by_current_model,"EXCLUDED. Dominated by growth, and the CPM has no nutrient-driven biomass creation: divide_cell! has no trigger and state.nutrient is never read by compute_delta_H. No quantity of thickening data can calibrate a model that cannot thicken."
specific_growth_rate,Population or biomass specific growth rate,none,Growth rate from OD or biovolume over time,false,true,false,false,declared,TIME_DECL_2026,unsupported_by_current_model,"EXCLUDED for two independent reasons: the model has no growth, and a parcel count is not a cell count."
